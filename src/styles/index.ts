import {StyleSheet} from 'react-native';
export const commonStyles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    gap: 16,
  },
  input: {
    width: 200,
    borderWidth: 1,
    color: '#32d583',
  },
});

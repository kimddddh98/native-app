import {StyleSheet} from 'react-native';
export const commonStyles = StyleSheet.create({
  container: {
    justifyContent: 'center',
    // gap: 0,
  },
  input: {
    width: 200,
    borderWidth: 1,
    color: '#32d581',
  },
});
